# .pre-commit-config.yamlrepos:- repo: https://github.com/pre-commit/pre-commit-hooks rev: v4.5.0 hooks: - id: check-added-large-files args: ['--maxkb=5000'] # 5MB 이상 파일 커밋 차단 (Gmail 용량 이슈 방지) - id: check-json # JSON 문법 오류 체크 (credentials.json 등 깨짐 방지) - id: check-yaml # YAML 문법 오류 체크 - id: detect-private-key # 개인키(Private Key) 포함 여부 감지 - id: end-of-file-fixer # 파일 끝에 엔터 없으면 자동 수정 - id: trailing-whitespace # 불필요한 공백 자동 제거# (고급) AWS, Google 키 패턴 감지- repo: https://github.com/gitleaks/gitleaks rev: v8.18.2 hooks: - id: gitleaks